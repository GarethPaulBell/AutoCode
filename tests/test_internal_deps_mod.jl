module TestInternalDeps

# Function: add_one
# Description: add one
add_one(x) = x + 1

# Function: use_add_one
# Description: use add_one
use_add_one(x) = add_one(x) * 2

# Function: add_one
# Description: add one
add_one(x) = x + 1

# Function: use_add_one
# Description: use add_one
use_add_one(x) = add_one(x) * 2

end # module TestInternalDeps
